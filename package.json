{
  "name": "@esnes/2c02",
  "version": "0.1.0",
  "module": "index.js",
  "devDependencies": {
    "rollup": "^0.64.0"
  },
  "scripts": {
    "build": "rollup src/render.js -o index.js -f es",
    "watch": "rollup src/render.js -o index.js -f es -w",
    "test": "node test/test.js",
    "pretest": "npm run build"
  }
}
